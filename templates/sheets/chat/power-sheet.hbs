<form class="{{cssClass}} age-system item power colorset-selection" autocomplete="off">

    <div class="sheet-content item-stats power">
        <div class="flexcol">
            {{#if inChat}}
            <div class="item-card-title colorset-first-tier">{{name}}</div>
            {{else}}
            <input name="name" class="item-card-title colorset-first-tier" type="text" value="{{item.name}}" placeholder="{{localize "age-system.item.name"}}"/>
            {{/if}}
            <div class="item-card-stats flex-justify-center flex-align-start flexrow">
                {{!-- Item Image --}}
                {{> "systems/age-system/templates/partials/item-image-sheet-card.hbs"}} 

                {{#if (ne inChat true)}}
                {{!-- Select Damage and Fatigue Options --}}
                <div class="power-type-control">
                    <a class="toggle-damage colorset-second-tier {{#unless data.system.hasDamage}}not-equiped{{/unless}}" title="{{localize "age-system.rollDamage"}}">
                        <div class="flexrow flex-justify-center flex-align-center">
                            <i class="fas fa-fist-raised"></i>
                        </div>
                    </a>
    
                    <a class="toggle-healing colorset-second-tier {{#unless data.system.hasHealing}}not-equiped{{/unless}}" title="{{localize "age-system.rollHealing"}}">
                        <div class="flexrow flex-justify-center flex-align-center">
                            <i class="fa fa-heartbeat"></i>
                        </div>
                    </a>

                    <a class="toggle-resist colorset-second-tier {{#unless data.system.hasTest}}not-equiped{{/unless}}" title="{{localize "age-system.item.testResist"}}">
                        <div class="flexrow flex-justify-center flex-align-center">
                            <i class="far fa-dot-circle"></i>
                        </div>
                    </a>

                    <a class="toggle-fatigue flexrow use-fatigue-true colorset-second-tier {{#unless data.system.hasFatigue}}not-equiped{{/unless}}" title="{{localize "age-system.rollFatigue"}}">
                        <div class="flexrow flex-justify-center flex-align-center">
                            <i class="fas fa-head-side-cough"></i>
                        </div>
                    </a>
                </div>
                {{/if}}
                
                {{!-- Ability/Focus to activate the power --}}
                {{> "systems/age-system/templates/partials/ability-focus-select.hbs"}}

                {{!-- Power Point Cost --}}
                {{#unless (and data.system.inputFatigueTN data.system.hasFatigue)}}
                {{#unless (and inChat data.system.hasFatigue)}}
                <div class="flexcol">
                    <label>{{localize "age-system.powerPointsIndex"}}</label>
                    {{#if inChat}}
                    <div>{{data.system.powerPointCostTotal}}</div>
                    {{else}}
                    <div class="flexrow">
                        <input type="text" name="system.powerPointCost" value="{{data.system.powerPointCost}}" data-dtype="Number" />
                        {{#if (ne data.system.powerPointCost data.system.powerPointCostTotal)}}
                        <div>({{data.system.powerPointCostTotal}})</div>
                        {{/if}}
                    </div>
                    {{/if}}
                </div>
                {{/unless}}
                {{/unless}}

                {{!-- Fatigue Setup --}}
                {{#if data.system.useFatigue}}
                {{#if (ne inChat true)}}       
                <div class="flexcol use-fatigue-true">
                    <label class="use-fatigue">{{localize "age-system.ablFatigue"}}</label>
                    <select name="system.ablFatigue" class="data-input">
                        {{#select data.system.ablFatigue}}
                        {{#each config.abilities as |name type|}}
                        <option value="{{type}}">{{localize name}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                </div>
                <div class="flexcol use-fatigue-true">
                    <label class="use-fatigue">{{localize "age-system.inputFatigueTN"}}</label>
                    <input class="use-fatigue" type="checkbox" name="system.inputFatigueTN" {{checked data.system.inputFatigueTN}} class="checkbox"/>
                </div>
                {{/if}}
                <div class="flexcol use-fatigue-true">
                    <label class="use-fatigue">{{localize "age-system.fatigueTN"}}</label>
                    <input class="use-fatigue" type="number" {{#unless data.system.inputFatigueTN}}disabled{{/unless}} name="system.fatigueTN" value="{{data.system.fatigueTN}}" data-dtype="Number" />                   
                </div>
                {{/if}}

                {{!-- Casting Time --}}
                <div class="flexcol">
                    <label>{{localize "age-system.item.cast"}} </label>
                    {{#if inChat}}
                    <div>{{localize (concat "age-system." data.system.castTime)}}</div>
                    {{else}}
                    <select name="system.castTime" class="data-input">
                    {{#select data.system.castTime}}
                        {{#each config.actionsToCast as |actionsToCast|}}
                        <option value="{{@key}}">{{localize actionsToCast}}</option>
                        {{/each}}
                    {{/select}}
                    </select>
                    {{/if}}
                </div>

                {{!-- Power Target Number --}}
                <div class="flexcol">
                    <label>{{localize "age-system.targetNumber"}} </label>
                    {{#if inChat}}
                    <div class="data-input">{{data.data.targetNumber}}</div>
                    {{else}}
                    <input type="number" class="data-input" name="system.targetNumber" value="{{data.system.targetNumber}}" data-dtype="Number" />
                    {{/if}}
                </div>

                {{!-- Power Force --}}
                <div class="flexcol">
                    <label>{{localize "age-system.item.force"}}</label>
                    <span class="data-input">{{data.system.itemForce}}</span>
                    {{#if (ne inChat true)}}
                    {{!-- Ability to activate item --}}
                    <div class="flexcol">
                        <select name="system.itemForceAbl" class="data-input">
                            {{#select data.system.itemForceAbl}}
                            {{#each config.abilities as |name type|}}
                            <option value="{{type}}">{{localize name}}</option>
                            {{/each}}
                            {{/select}}
                        </select>
                    </div>
                    {{/if}}             
                </div>

                {{!-- Resist Test --}}
                {{#if data.system.hasTest}}
                {{#if inChat}}
                <div class="flexcol flex-align-center">
                    <label style="white-space: nowrap;">{{localize "age-system.item.testResist"}}</label>
                    <p style="white-space: nowrap;">{{localize (concat "age-system." data.system.testAbl)}} ({{data.system.testFocus}})</p>
                </div>
                {{else}}
                <div class="flexcol">
                    <label>{{localize "age-system.item.testResist"}}</label>
                    {{!-- Ability to Resist Power --}}
                    <select name="system.testAbl" class="data-input">
                        {{#select data.system.testAbl}}
                        {{#each config.abilities as |name type|}}
                        <option value="{{type}}">{{localize name}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                    {{!-- Focus to Resist Power --}}
                    <input name="system.testFocus" class="data-input" type="text" value="{{data.system.testFocus}}" placeholder="{{localize "age-system.focus"}}" list="{{item.type}}-{{item._id}}-list-resist"/>
                    <datalist id="{{item.type}}-{{item._id}}-list-resist">
                        <option value=""></option>
                        {{#each config.focus as |focus|}}
                        <option value="{{focus.name}}"></option>
                        {{/each}}
                    </datalist>
                </div>
                {{/if}}
                {{/if}}

                {{!-- Power Damage/Heal --}}
                {{#if (or data.system.causeDamage data.system.causeHealing)}}
                {{> "systems/age-system/templates/partials/dmg-block-sheet.hbs"}}
                {{/if}}

            </div>

            {{!-- Active Bonuses --}}
            {{> "systems/age-system/templates/partials/active-bonuses.hbs"}}

            {{#if inChat}}
            {{!-- Description --}}
            <div class="item-card-desc">
                <label>{{localize "age-system.item.desc"}}</label>
                {{editor data.system.longDesc target="system.longDesc" button=true owner=owner editable=true}}
            </div>
            {{/if}}

        </div>

        {{#if (ne inChat true)}}
        {{!-- Tab Control --}}
        {{> "systems/age-system/templates/partials/bonus-desc-sheet.hbs"}}
        {{/if}}

    </div>
</form>
{{#if inChat}}
    {{> "systems/age-system/templates/partials/item-card-buttons.hbs"}}
{{/if}}